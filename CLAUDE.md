# ТӨСЛИЙН ГАРЫН АВЛАГА БА АЖИЛЛАХ ДҮРЭМ (festival-tuluvluguu)

> **ЧУХАЛ:** Шинэ чат эхлэх бүрд энэ дүрмийг уншиж, мөрдлөг болгоно. **Дараа нь заавал `AI_HISTORY.md`-ийн эхний 10-15 мөрийг уншиж, өмнөх чатуудад юу хийгдсэнийг мэдэж авна** (§7).
> **PUSH = DEPLOY:** "push хий" гэсэн хүсэлт нь push + Netlify deploy-г хамтад нь гэсэн үг — дунд нь дахин зөвшөөрөл асуухгүй (§6).
> **⚡ ТОКЕН ХЭМНЭХ / ХУРДАН АЖИЛЛАХ ЗААВАР:** `index.html` нэг файл ~500 мөр тул бүтнээр бүү унш — `grep` болон `Read offset/limit` ашигла. Зассан файлыг дахин уншиж токен бүү үрэ (Edit амжилттай болсон бол хангалттай). Шалгалтуудыг нэг Bash комманд (`tools/check.sh`) дотор багцалсан — тусад нь `node --check` гэх мэтийг гараар давтаж бүү бич. Хариулт маш товч, тодорхой байна.

## 1. Юу вэ (Товч танилцуулга)
Энэ бол **Дорноговь аймгийн Засаг даргын Тамгын газраас өгсөн үүрэг, чиглэлийн биелэлтийн хяналтын** статик вэбсайт.
- Эх сурвалж 2 хурлын тэмдэглэл: **2026.07.09** (Аймгийн Засаг даргаас өгсөн үүрэг, чиглэл, 27 зүйл) ба **2026.07.20** (Ерөнхий сайдын хуралдаанаас өгсөн үүрэг, чиглэл — өвөлжилтийн бэлтгэл сэдвээр, 24 зүйл). Нийт **51 үүрэг, чиглэл**.
- Ихэнх зүйлд тодорхой эцсийн хугацаа байдаггүй — зөвхөн хурлын огноо (07.09 эсвэл 07.20) л байна. Зөвхөн 2 зүйлд эх бичвэрт дурдсан тодорхой эцсийн хугацаа бий (08.20, 09.15) — эдгээрийг `RAW`-д `"07.20-08.20"` маягийн **огнооны муж** (issue → due) байдлаар тэмдэглэсэн.
- Хуанли / Хугацааны шугам (Gantt) / Жагсаалт гэсэн 3 харагдацаар үзүүлнэ. Ажил бүрийг эх сурвалж (хурал) хийгээд огноогоор нь эрэмбэлдэг — өмнөх хувилбарт байсан "16 ажлын хэсэг" гэдэг нь **байхгүй болсон** (устгагдсан), одоо зөвхөн **2 эх сурвалж** (SECTIONS) байна.
- ⚠️ Энэ сайт өмнө нь "Тэнгэрийн хутагт" фестивалийн төлөвлөгөө байсан бөгөөд хэрэглэгчийн хүсэлтээр **тэр агуулга бүрэн устгагдаж**, одоогийн үүрэг-чиглэлийн агуулгаар солигдсон (Git түүхэнд хуучин хувилбар үлдсэн). Дахин ижил төстэй солилт хийхэд өмнөх агуулгыг сэргээхийг бүү оролд — зөвхөн хэрэглэгчийн шинэ зааврыг дага.

## 2. Техникийн үндэс ба орчин
- **Framework: байхгүй.** Цэвэр HTML + CSS + vanilla JS — **бүх зүйл нэг файл `index.html`-д** байрлана (inline `<style>`, inline `<script>`). Build step, npm dependency шаардлагагүй.
- **Document root:** repo-ийн үндэс өөрөө (`public/` гэж байхгүй). Netlify `--dir=.`-ээр шууд deploy хийдэг.
- **Хэл:** Бүх UI текст Монгол, шууд HTML дотор бичигдсэн (орчуулгын key/файл байхгүй).
- **UI/Брэнд:** "Галт/жаран" (fire/ember) өнгө схем хэвээр үлдсэн (агуулга солигдсон ч визуал брэндийг хэрэглэгч тусгайлан өөрчлөх хүсэлт өгөөгүй) — CSS variables `:root` дотор (`--accent:#e6431f`, `--gold`, `--night` гэх мэт), фонт **Rubik** (Google Fonts, Cyrillic дэмждэг). Эх сурвалж (хурал) тус бүрийн өнгө `PALETTE` массивт (2 элемент).

## 3. Кодын бүтэц (index.html дотор хайх зураглал)
| Хэсэг | Хаана хайх (grep) |
|---|---|
| Өнгө/CSS хувьсагч | `grep -n ":root" index.html` |
| Header/hero HTML | `grep -n "header class=\"top\"" index.html` |
| Статистик картууд | `grep -n "id=\"stats\"" index.html` |
| Хайлт/шүүлтүүр (controls) | `grep -n "class=\"controls\"" index.html` |
| 2 эх сурвалж (хурал)-ийн нэрс | `grep -n "const SECTIONS" index.html` |
| Эх сурвалжийн өнгөнүүд | `grep -n "const PALETTE" index.html` |
| **Бүх үүрэг, чиглэлийн өгөгдөл** | `grep -n "const RAW = \[" index.html` (мөр бүр = 1 үүрэг/чиглэл) |
| Огноо задлах логик | `grep -n "function parsePeriod" index.html` |
| Хурлын өдөр/эцсийн хугацааны highlight | `grep -n "MEETING_DATES\|DUE_DATES" index.html` |
| Хуанли харагдац (3 сар: 7,8,9) | `grep -n "function renderCalendar" index.html` |
| Хугацааны шугам (Gantt) | `grep -n "function renderTimeline" index.html` |
| Жагсаалт (хүснэгт) | `grep -n "function renderList" index.html` |
| Дэлгэрэнгүй цонх (modal) | `grep -n "function openModal" index.html` |
| Харагдац солих (tab) | `grep -n "function setView" index.html` |

## 4. Код бичих дүрэм
- **Шинэ үүрэг/чиглэл нэмэх:** `RAW` массивт мөр нэм — `[sectionId, "чиглэлийн текст", "07.09" эсвэл эцсийн хугацаатай бол "07.20-09.15" форматтай огноо, "хариуцагч (эсвэл \"\")", "хяналт тавигч (эсвэл \"\")", "тайлбар (эсвэл \"\")"]`. `id` талбар автоматаар индексээр олгогддог тул **гараар бүү тавь**. Хариуцагч/хяналт тавигч эх баримт бичигт бөглөгдөөгүй бол **бүү зохиож бич** — хоосон `""` үлдээ.
- **Шинэ эх сурвалж (хурал) нэмэх бол** `SECTIONS` **БОЛОН** `PALETTE`-ийг хамт шинэчил (урт нь тэнцүү байх ёстой, эс бөгөөс `sectionColor()` mod-аар өнгө давхардана). `tools/check.sh` одоо `SECTIONS.length`-ээс динамикаар шалгадаг тул тоо өөрчлөгдөхөд шалгалт өөрөө зохицно.
- **Огнооны формат:** зөвхөн `MM.DD` (ганц огноо, жишээ нь хурлын огноо) эсвэл `MM.DD-MM.DD` (эхлэх→дуусах хугацаа, зөвхөн тодорхой эцсийн хугацаа мэдэгдэж байгаа тохиолдолд) — жил үргэлж 2026, `const YEAR` тогтмолоор. Эх баримтад "9 сард багтаан" мэтийн тодорхой бус хугацаа байвал **огноо биш, `тайлбар` (note) талбарт л бич** — параметр буруу задрахаас сэргийл.
- Хурлын өдрүүд (`MEETING_DATES`) болон эцсийн хугацаатай өдрүүд (`DUE_DATES`) хуанли/timeline дээр өөр өөр highlight-тэй (`.main-event` / `.due-day` CSS class). Шинэ хурал/эцсийн хугацаа нэмэгдвэл эдгээр массивыг шинэчил.
- **Өнгө өөрчлөх бол** `:root`-ийн CSS variable-ийг л засах — тусдаа элементэд hex өнгө шинээр бүү шигтгэ.
- Хэрэглэгчийн өгөгдлийг (`it.t`, `it.r`, `it.n`...) HTML руу оруулахдаа **үргэлж `escapeHtml()`** ашигла (XSS-с сэргийлэх дадал).
- Тусдаа `.css`/`.js` файл бүү үүсгэ — нэг файлын энгийн бүтцийг хадгал (deploy хялбар болгоно).

## 5. Гүйцэтгэлийн баталгаа (Verification loop) — ЗААВАЛ
Засвар хийсний дараа **дараах нэг коммандыг** ажиллуулж, бүгд ✅ болтол дахин давтана:
```bash
bash tools/check.sh
```
Энэ нь дараах 3 зүйлийг нэг дор шалгана:
1. `<script>` доторх JS syntax (`node --check`).
2. `<style>` доторх CSS хаалтын `{ }` тэнцвэр.
3. `RAW` массив доторх бүх огноо (`parsePeriod`) болон section id (1–`SECTIONS.length`) зөв эсэх.

Алдаа гарвал засаад дахин `bash tools/check.sh` ажиллуулна. Бүгд ✅ болсны дараа commit хийнэ.
(UI-г нүдээр шалгах бол: `python3 -m http.server 8080` асаагаад browser-оор шалгаж болно.)

## 6. Git ба Deploy урсгал
- **GitHub:** https://github.com/Boldsaikhan/festival-tuluvluguu (private), үндсэн branch `main`. (Repo нэр хуучин фестивалийн агуулгаас үлдсэн — агуулга солигдсон ч нэрийг өөрчлөөгүй.)
- **Netlify:** production https://pp-100.netlify.app (`pp` нэр аль хэдийн эзэмшигдсэн байсан тул `pp-100` болсон). Site холбоос локал `.netlify/state.json`-д хадгалагдсан (`.gitignore`-д орсон тул commit хийгдэхгүй — шинэ машин дээр `npx netlify-cli link --id 3345998a-7929-4697-b330-256d362b2cdb` хийж холбоно).
- **⚠️ GitHub → Netlify автомат холбоос ХИЙГДЭЭГҮЙ.** `git push` хийхэд Netlify өөрөө шинэчлэгдэхгүй — заавал тусад нь дараах коммандыг ажиллуул:
  ```bash
  npx --yes netlify-cli deploy --prod --dir=.
  ```
- **🔒 PUSH = DEPLOY (2026-07-29-нээс мөрдөх дүрэм).** Хэрэглэгч "push хий" гэж хүсвэл энэ нь **push + Netlify deploy-г хамтад нь** гэсэн үг. Push хийчхээд deploy хийх эсэхийг дахин **бүү асуу** — доорх дарааллыг нэг мөсөн гүйцэтгэ:
  ```bash
  bash tools/check.sh                       # 1) бүгд ✅ байх ёстой
  git add <зөвхөн засварласан файлууд>       # 2) git add -A / . БҮҮ хэрэглэ
  git commit -m "…"                          # 3) монгол хэлээр
  git push                                   # 4)
  npx --yes netlify-cli deploy --prod --dir=. # 5) ЗААВАЛ — GitHub→Netlify автомат холбоос БАЙХГҮЙ
  ```
  Дараа нь `AI_HISTORY.md`-д commit hash + "Push, Netlify deploy хийв" гэж бөглөж, тэр мөрийг бас push хийнэ.
- **Гэхдээ push-ыг өөрөө санаачлахгүй:** commit хүртэл өөрөө хийж болно, харин `git push` нь нийтэд харагдах үйлдэл тул **хэрэглэгч хүсээгүй байхад бүү эхлүүл** — зөвхөн "push хий" / "deploy хий" гэж хэлсэн үед. Хүссэн үед нь бол дээрх 5 алхмыг асуултгүй бүрэн гүйцэтгэнэ.

## 7. Харилцах хэл ба түүх хөтлөх (ЧАТ ХООРОНДЫН ОЙ САНАМЖ)
- Бүх тайлбар, мэдээлэл, хариулт болон Git commit message-ийг **үргэлж монгол хэлээр** бичнэ (энэ хэрэглэгчийн бүх төсөл/чатад мөрдөх ерөнхий зарчим).
- **🔒 ШИНЭ ЧАТ ЭХЛЭХЭД (ЗААВАЛ):** энэ `CLAUDE.md`-г уншсаны дараа **`AI_HISTORY.md`-ийн эхний 10-15 мөрийг унш**. Тэнд өмнөх чатуудад юу хийгдсэн, ямар шийдвэр гарсан, юу дутуу үлдсэн нь бичигдсэн байна. Ажил эхлэхээсээ өмнө үүнийг мэдэж байх ёстой — өмнө хийсэн зүйлийг давтах, эсвэл цуцалсан шийдлийг дахин санал болгохоос сэргийлнэ.
- **🔒 ЧАТ БҮРИЙН ҮР ДҮНГ ХАДГАЛАХ (ЗААВАЛ):** аливаа үйлдэл, засвар дууссаны дараа **тэр даруй** `AI_HISTORY.md`-д нэг мөр нэмнэ (хамгийн шинэ нь дээрээ):
  `| Огноо | Хийсэн ажил | Гол файл(ууд) | Commit |`
  - "Хийсэн ажил" баганад **юу, яагаад, хаана** гэдгийг тодорхой бич (функц/CSS class/хувьсагчийн нэр дурдвал шинэ чат шууд олно). "Засвар хийв" гэх мэт ерөнхий бичиглэл хангалтгүй.
  - Хэрэглэгчийн **шийдвэр, сонголт, өгсөн заавар**-ыг бас тэмдэглэ (жишээ нь "хэрэглэгч X биш Y-г сонгов", "Z-г хийхгүй гэж хэлсэн") — эдгээр нь кодоос уншигдахгүй мэдээлэл тул зөвхөн энд үлдэнэ.
  - Дуусаагүй/дутуу зүйл байвал ⚠️ тэмдэгтэй бич (жишээ нь `DEPT_BY_RESP` зураглал хоосон хэвээр).
  - Commit хийгээгүй бол `*(commit хийгээгүй)*` гэж тавиад, commit/push хийсний дараа hash-аар солино.
- Хэрэв `AI_HISTORY.md` файл 40+ мөрөөс давж томрвол хуучин мөрүүдийг токен хэмнэх зорилгоор `AI_HISTORY_archive.md` руу шилжүүлж архивлана.
