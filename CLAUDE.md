# ТӨСЛИЙН ГАРЫН АВЛАГА БА АЖИЛЛАХ ДҮРЭМ (festival-tuluvluguu)

> **ЧУХАЛ:** Шинэ чат эхлэх бүрд энэ дүрмийг уншиж, мөрдлөг болгоно.
> **⚡ ТОКЕН ХЭМНЭХ / ХУРДАН АЖИЛЛАХ ЗААВАР:** `index.html` нэг файл ~700 мөр тул бүтнээр бүү унш — `grep` болон `Read offset/limit` ашигла. Зассан файлыг дахин уншиж токен бүү үрэ (Edit амжилттай болсон бол хангалттай). Шалгалтуудыг нэг Bash комманд (`tools/check.sh`) дотор багцалсан — тусад нь `node --check` гэх мэтийг гараар давтаж бүү бич. Хариулт маш товч, тодорхой байна.

## 1. Юу вэ (Товч танилцуулга)
Энэ бол **"Тэнгэрийн хутагт" аялал жуулчлалын фестивалийн (2026, Дорноговь аймаг, Сайншанд)** зохион байгуулах ерөнхий төлөвлөгөөг **календарчилсан** харагдацаар үзүүлдэг статик вэбсайт.
- **16 ажлын хэсэг, ~158 үйл ажиллагааг** огноогоор нь Хуанли / Хугацааны шугам (Gantt) / Жагсаалт гэсэн 3 харагдацаар үзүүлнэ.
- Эх сурвалж: АЗДТГ-ын баталсан ерөнхий төлөвлөгөө (Word/зураг хэлбэрээр өгөгдсөн, гараар `index.html`-ийн `RAW` массив руу шилжүүлсэн).

## 2. Техникийн үндэс ба орчин
- **Framework: байхгүй.** Цэвэр HTML + CSS + vanilla JS — **бүх зүйл нэг файл `index.html`-д** байрлана (inline `<style>`, inline `<script>`). Build step, npm dependency, tusgai шаардлагагүй.
- **Document root:** repo-ийн үндэс өөрөө (`public/` гэж байхгүй). Netlify `--dir=.`-ээр шууд deploy хийдэг.
- **Хэл:** Бүх UI текст Монгол, шууд HTML дотор bичигдсэн (орчуулгын key/файл байхгүй).
- **UI/Брэнд:** "Галт/жаран" (fire/ember) фестивалийн өнгө схем — CSS variables `:root` дотор (`--accent:#e6431f`, `--gold`, `--night` гэх мэт), фонт **Rubik** (Google Fonts, Cyrillic дэмждэг). Section тус бүрийн өнгө `PALETTE` массивт (16 элемент).

## 3. Кодын бүтэц (index.html дотор хайх зураглал)
| Хэсэг | Хаана хайх (grep) |
|---|---|
| Өнгө/CSS хувьсагч | `grep -n ":root" index.html` |
| Header/hero HTML | `grep -n "header class=\"top\"" index.html` |
| Статистик картууд | `grep -n "id=\"stats\"" index.html` |
| Хайлт/шүүлтүүр (controls) | `grep -n "class=\"controls\"" index.html` |
| 16 ажлын хэсгийн нэрс | `grep -n "const SECTIONS" index.html` |
| Section өнгөнүүд | `grep -n "const PALETTE" index.html` |
| **Бүх үйл ажиллагааны өгөгдөл** | `grep -n "const RAW = \[" index.html` (мөр бүр = 1 activity) |
| Огноо задлах логик | `grep -n "function parsePeriod" index.html` |
| Хуанли харагдац | `grep -n "function renderCalendar" index.html` |
| Хугацааны шугам (Gantt) | `grep -n "function renderTimeline" index.html` |
| Жагсаалт (хүснэгт) | `grep -n "function renderList" index.html` |
| Дэлгэрэнгүй цонх (modal) | `grep -n "function openModal" index.html` |
| Харагдац солих (tab) | `grep -n "function setView" index.html` |

## 4. Код бичих дүрэм
- **Шинэ activity нэмэх:** `RAW` массивт мөр нэм — `[sectionId(1-16), "текст", "07.22-08.01" эсвэл "08.05" форматтай огноо", "хариуцагч", "хамтрагч (эсвэл \"\")", "тайлбар (эсвэл \"\")"]`. `id` талбар автоматаар индексээр олгогддог тул **гараар бүү тавь**.
- **Шинэ section нэмэх/өөрчлөх бол** `SECTIONS` **БОЛОН** `PALETTE` хоёрыг хамт шинэчил (`PALETTE`-ийн урт `SECTIONS`-ээс бага бол өнгө давхардана — `sectionColor()` mod ашигладаг).
- **Огнооны формат:** зөвхөн `MM.DD` эсвэл `MM.DD-MM.DD` (жил үргэлж 2026, `const YEAR` тогтмолоор). Өөр формат бичвэл `parsePeriod()` буруу задална — заавал `tools/check.sh`-аар шалга.
- **Өнгө өөрчлөх бол** `:root`-ийн CSS variable-ийг л засах — тусдаа элементэд hex өнгө шинээр бүү шигтгэ (theme уялдаагүй болно).
- Хэрэглэгчийн өгөгдлийг (`it.t`, `it.r`, `it.n`...) HTML руу оруулахдаа **үргэлж `escapeHtml()`** ашигла (XSS-с сэргийлэх дадал).
- Тусдаа `.css`/`.js` файл бүү үүсгэ — нэг файлын энгийн бүтцийг хадгал (deploy хялбар болгоно).

## 5. Гүйцэтгэлийн баталгаа (Verification loop) — ЗААВАЛ
Засвар хийсний дараа **дараах нэг коммандыг** ажиллуулж, бүгд ✅ болтол дахин давтана:
```bash
bash tools/check.sh
```
Энэ нь дараах 3 зүйлийг нэг дор шалгана:
1. `<script>` доторх JS syntax (`node --check`).
2. `<style>` доторх CSS хаалтын `{ }` тэнцвэр.
3. `RAW` массив доторх бүх огноо (`parsePeriod`) болон section id (1–16) зөв эсэх.

Алдаа гарвал засаад дахин `bash tools/check.sh` ажиллуулна. Бүгд ✅ болсны дараа commit хийнэ.
(UI-г нүдээр шалгах бол: `python3 -m http.server 8080` асаагаад browser-оор шалгаж болно.)

## 6. Git ба Deploy урсгал
- **GitHub:** https://github.com/Boldsaikhan/festival-tuluvluguu (private), үндсэн branch `main`.
- **Netlify:** production https://pp-100.netlify.app (`pp` нэр аль хэдийн эзэмшигдсэн байсан тул `pp-100` болсон). Site холбоос локал `.netlify/state.json`-д хадгалагдсан (`.gitignore`-д орсон тул commit хийгдэхгүй — шинэ машин дээр `npx netlify-cli link --id 3345998a-7929-4697-b330-256d362b2cdb` хийж холбоно).
- **⚠️ GitHub → Netlify автомат холбоос ХИЙГДЭЭГҮЙ.** `git push` хийхэд Netlify өөрөө шинэчлэгдэхгүй — заавал тусад нь дараах коммандыг ажиллуул:
  ```bash
  npx --yes netlify-cli deploy --prod --dir=.
  ```
- **Push/Deploy-г хэзээ хийх:** `bash tools/check.sh` бүгд ✅ болсны дараа л commit → push → netlify deploy хийнэ. Push болон deploy нь GitHub/live сайтад нийтэд харагдах өөрчлөлт учир **хэрэглэгчээс тодорхой зөвшөөрөл асуухгүйгээр автоматаар бүү хий** — зөвхөн хэрэглэгч тухайлан "push хий", "deploy хий" гэж хүссэн үед гүйцэтгэ.

## 7. Харилцах хэл ба түүх хөтлөх
- Бүх тайлбар, мэдээлэл, хариулт болон Git commit message-ийг **үргэлж монгол хэлээр** бичнэ.
- Аливаа үйлдэл, засвар дууссаны дараа үр дүнг **`AI_HISTORY.md`** файл дээр дараах бүтцээр үргэлж нэг мөр нэмж тэмдэглэнэ (Хамгийн шинэ нь дээрээ байрлана):
  `| Огноо | Хийсэн ажил | Гол файл(ууд) | Commit |`
- Хэрэв `AI_HISTORY.md` файл 40+ мөрөөс давж томрвол хуучин мөрүүдийг токен хэмнэх зорилгоор `AI_HISTORY_archive.md` руу шилжүүлж архивлана.
